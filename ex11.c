#include <stdio.h>
#include <math.h>

int radice_quadrata(int n)
{
  

}

int main() 
{
    float a;
     float b;
    float c;
    float delta;
    printf("inserisci i valori di a, b, c");
    scanf("%f", &a);
    scanf("%f", &b);
    scanf("%f", &c);
    delta== b*b-4*a*c;
    if(delta>0)
    {
        printf()
    }

    
}
